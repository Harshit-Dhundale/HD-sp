---
title: "Chapter 6 – Quantum Circuits & Edge Cases"
description: "Dragging gates, handling collisions, and post-interview polish."
coverIllustration: "/images/ch6-quantum-gates.png"
yearFrom: "2025-07"
yearTo: "2025-07"
---

import PageSpread from '@/components/PageSpread'
import StoryCard from '@/components/StoryCard'
import TimelineBubble from '@/components/TimelineBubble'
import ParallaxLayer from '@/components/ParallaxLayer'
import EasterEggLink from '@/components/EasterEggLink'
import Link from 'next/link'
import { ChevronLeft, ChevronRight } from 'lucide-react'

<div className="relative min-h-screen">
  <ParallaxLayer speed={0.5} className="opacity-20">
    <div className="absolute inset-0">
      <div className="absolute top-1/4 left-1/3 w-2 h-2 bg-gold/40 rounded-full animate-pulse" />
      <div className="absolute bottom-1/3 right-1/4 w-3 h-3 bg-mint/40 rounded-full animate-pulse" />
      <div className="absolute top-1/2 left-1/4 w-1 h-1 bg-gold/60 rounded-full animate-pulse" />
    </div>
  </ParallaxLayer>

  <PageSpread chapterIndex={6}>
    <div className="prose prose-invert max-w-none">
      <h2 className="text-3xl font-display text-gold mb-6">Collisions in Superposition</h2>
      
      <TimelineBubble year="July 2025" />
      
      <p className="text-lg leading-relaxed text-parchment/90 mb-6">
        I entered Bloq Quantum's interview <em>dragging virtual gates on a grid</em>. 
        They spotted an unhandled edge case— <strong>What if a gate expands near the layout's edge?</strong>
      </p>

      <p className="text-lg leading-relaxed text-parchment/90">
        That night, I fixed it, pushed to GitHub, redeployed, and sent a polite follow-up email. 
        <em>Attention to detail</em> matters when qubits (and careers) are at stake.
      </p>
    </div>

    <div className="space-y-6">
      <StoryCard
        emoji="🔬"
        heading="Quantum Circuit Builder"
        bullets={[
          "React + Tailwind drag-&-drop grid",
          "Dynamic gate width collision algo",
          "Edge-case fix delivered post-interview",
          "Live demo hosted on Vercel"
        ]}
      >
        <div className="mt-4">
          <EasterEggLink 
            href="https://bloq-quantum-circuit.vercel.app"
            className="text-link-hover hover:underline text-sm"
          >
            🔗 Try the Circuit Builder
          </EasterEggLink>
        </div>
      </StoryCard>
      
      <div className="flex justify-between items-center pt-8">
        <Link 
          href="/hackathon-leaps"
          className="flex items-center gap-2 px-4 py-2 text-gold hover:text-gold/80 transition-colors"
        >
          <ChevronLeft className="w-4 h-4" />
          Previous
        </Link>
        <Link 
          href="/think41-chatbot"
          className="flex items-center gap-2 px-4 py-2 text-gold hover:text-gold/80 transition-colors"
        >
          Next Chapter
          <ChevronRight className="w-4 h-4" />
        </Link>
      </div>
    </div>
  </PageSpread>
</div>
\`\`\`
